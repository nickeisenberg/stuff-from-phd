\documentclass[a4paper,10pt]{article}
\usepackage[utf8]{inputenc}
\RequirePackage{amsthm,amsmath,amssymb,amscd,xcolor}
\def\D{^{C}D_t^\alpha}
\def\W{\mathbb{W}}
\def\H{\mathbb{H}}
\def\K{\mathbb{K}}
\def\E{\mathbb{E}}
\def\cL{\mathcal{L}}
\def\R{\mathbb{R}}
\def\ud{\text{d}}
\newcommand{\Norm}[1]{\left|\left|  #1   \right|\right|}
\begin{document}

Here are comments for ef9867aa on the file -- {\it thesis.tex}:

\begin{enumerate}
  \item Beginning of Section 4,
    \begin{enumerate}
      \item equation 1.1 should be equation (1.1);
      \item $W$ is a 0-mean $\to$ $\dot{W}$ is a centered;
      \item indexed on $\to$ indexed by;
    \end{enumerate}
  \item Last centered equation on p. 11, the second $\R^{2d}$ should be $\R^d$.
  \item Part 1 of Assumption 4.1, absolute continuity of $\Gamma$ doesn't imply that of $\mu$. Think about the case when
    $\Gamma=\text{Leb.}$ If you do want the second part, you need to assume that $\mu$ is also absolutely continuous
    w.r.t. Leb. Or probably you need to assume only one of these two?
  \item Lemma 4.5:
    \begin{enumerate}
      \item The statement, ``for all $t>0$ and $x,y\in\R^d$".
      \item You forgot period for Eq. (4.6); check all such typos.
      \item Remove the extra period in the second last centered equation in the proof.
      \item The last centered equation, should $\le$ be $=$?
    \end{enumerate}
  \item Theorem 4.6:
    \begin{enumerate}
      \item $\dot{F}$ should be $\dot{W}$.
      \item First centered equation, add ``for $n\ge 1$".
      \item You forgot $\Norm{\cdots}_{\textcolor{magenta}{2}}$ in the 5th centered equation in the proof.
      \item One has to make proper assumptions on initial conditions. You didn't show how $ J_0(t,x)$ plays a role in
        the iteration. If initial condition are bounded, one needs only check Dalang's condition and cite Dalang 99
        paper for this standard result.
      \item Conjecture 4.9:
        \begin{enumerate}
          \item The assumption for being in $L^1(\R^d)$ seems too much, which exclude constant initial condition.
          \item After the first line of the first centered equation, did you try to apply the intermediate theorem on
             $Z$?
        \end{enumerate}
    \end{enumerate}
\end{enumerate}

\end{document}
